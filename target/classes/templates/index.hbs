{{#partial "content"}}
<div class="container">
    <select id="choix" name="choix" class="box">
        <option value="en" id="select_en">En</option>
        <option value="fr" id="select_fr">Fr</option>
    </select>
</div>
<a href="/" class="link-home">HOME</a>
{{#if categories.englich}}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item active" aria-current="page">Category</li>
    </ol>
</nav>
<section class="job-domaines-section p-tp-90 p-btm-120">
    <h2 class="section-title m-bt-50 text-center">Category Pokemons</h2>
      <div class="container">
<p class="paragraph-section">
  Egg Groups are categories which determine which Pokémon are able to interbreed. Pokémon may belong to either one or two Egg Groups.
</p>
  
        <div class=" row job-domaines">
            {{#each categories.categoriesPokemons}}
            <div class="col-lg-3 col-md-6 job-domaine">
                <div class="domaine-content" onclick="window.location.href='/categories/en/{{this.name}}'">
                    <h4 class="domaine-title">{{this.name}}</h4>
                    <img src="/assets/images/eggs-poke.png">
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>
{{/if}}
{{#if categories.french}}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item active" aria-current="page">Categories</li>
    </ol>
</nav>
<section class="job-domaines-section p-tp-90 p-btm-120">
    <h2 class="section-title m-bt-50 text-center">les Categories des Pokemons</h2>
       <div class="container">
    <p class="paragraph-section">
Les groupes d'œufs sont des catégories qui déterminent quels Pokémon sont capables de se croiser. Les Pokémon peuvent appartenir à un ou deux groupes d'œufs.</p>

        <div class=" row job-domaines">
            {{#each categories.stringList}}
            <div class="col-lg-3 col-md-6 job-domaine">
                <div class="domaine-content"
                    onclick="window.location.href='/categories/fr/{{@index_1}}?group={{this}}'">
                    <h4 class="domaine-title">{{this}}</h4>
                    <img src="/assets/images/eggs-poke.png">
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>
{{/if}}

{{/partial}}
{{> template.hbs}}